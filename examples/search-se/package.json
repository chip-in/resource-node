{
  "name": "@chip-in/resourcenode-example-search-se",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node lib/index.js",
    "cleanbuild": "npm run clean && npm run build",
    "build": "babel src -s inline -d lib/",
    "clean": "del-cli lib"
  },
  "author": "",
  "dependencies": {
    "express": "^4.16.1",
    "request": "^2.87.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "cpx": "^1.5.0",
    "del-cli": "^1.1.0"
  }
}
