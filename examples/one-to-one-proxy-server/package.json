{
  "name": "@chip-in/resourcenode-example-one-to-one-proxy-server",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "cleanbuild": "npm run clean && npm run build",
    "build": "babel src -s inline -d lib/",
    "clean": "rimraf lib",
    "watch": "npm run clean && babel src -s inline -d lib/ -w",
    "prepare": "npm run cleanbuild"
  },
  "author": "",
  "dependencies": {
    "express": "^4.16.1",
    "request": "^2.87.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.11.6",
    "@babel/core": "^7.11.6",
    "@babel/preset-env": "^7.11.5",
    "rimraf": "^3.0.2"
  }
}
